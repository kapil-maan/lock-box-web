/** @type {import('next').NextConfig} */
const nextConfig = {
  // This line is the key. It tells Next.js to output a static site.
  output: 'export',
  basePath: '/lock-box-web',
};

export default nextConfig;
